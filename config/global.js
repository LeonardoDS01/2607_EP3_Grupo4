module.exports={
    secret: 'mysecrettext',
    port: 3000,
    time24h: 60*60*24,
    emailRegex: /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/,
    messageCode200: 'Operación realizada con éxito',
    messageCode500: 'Operación fallida'
}